@packiko:registry=https://npm.pkg.github.com
# Auth token is NOT committed. Set it yourself (see README) — a GitHub token with
# read:packages scope, exported as NODE_AUTH_TOKEN (the var this npmrc reads):
#   export NODE_AUTH_TOKEN=$(gh auth token)
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
